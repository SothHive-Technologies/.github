# SothHive Technologies — org profile

This repository is named `.github` so GitHub can render **[profile/README.md](profile/README.md)** on the organization homepage.

Upload `social-preview.png` in the org **Settings → General** social preview. Pin repositories from the org sidebar.
